# 2026-08-09

## 项目架构评估：个人 + 团队双模式
- WorkFollow 已完成个人+团队双模式落地（README 滞后，仍描述为纯个人工具）。
- 架构要点：个人资源 owner_id 隔离；团队 team_id 归集 + TeamMember 三级 RBAC（OWNER/ADMIN/MEMBER）；个人→团队三条通道（TeamNoteSubmission 快照审批、TeamFileAccess 附件授权、NoteShare 只读分享）；Cookie 会话认证（workfollow_session，30 天）；9 类通知 + AuditLog 审计。
- 技术栈：FastAPI + SQLAlchemy 2 + Alembic（迁移至 0014）+ SQLite；前端 Vue 3.5 + TS + Pinia（workspace store 切 PERSONAL/TEAM）+ Tiptap + FullCalendar。
- 已向用户提出改进建议：聚合"我的任务"收件箱、team_tasks 补 list_name/tags、空间切换器、"待办移交团队"动作、审批角标、补 Docker/PostgreSQL 路径。
- 杂物：backend/workfollow.db 为 0 字节误生成文件可删；docker/ 目录为空。

## 实施：个人+团队体验改进（全部完成）
- 迁移 0015：team_tasks 增加 list_name/tags；todos 增加 transferred_team_task_id（索引 ix_todos_transferred_team_task_id）。
- 新端点 POST /api/todos/{id}/transfer：个人待办转团队任务（需 OWNER/ADMIN），client_request_id="todo-transfer:{todoId}" 幂等；团队任务完成时 recompute_task_status 内 _sync_transferred_todos 自动完成原待办。
- TodoSourceType 增加 TEAM；TeamTaskCreate/Update/Read、MyTaskRead 增加 list_name/tags；team_tasks 路由 _task_read 改名 task_read 供 todos 路由复用。
- 前端：新组件 TransferTodoDialog.vue；TodoItem 加"移交给团队"菜单项和"已移交"chip；TeamTasksPage 表单加清单/标签；AppSidebar 切换器上移到导航上方、显示当前空间首字、通知加未读角标（60s 轮询）。
- 发现：聚合"我的任务"此前已在 TodosPage 实现（团队分配分组 + /api/my/tasks），无需重复建设。
- README 已补双模式说明；删除 0 字节 backend/workfollow.db。
- 验证：后端 51 tests passed（含新增 test_todo_transfer.py 3 个），前端 vue-tsc + vitest + vite build 全绿。

## 侧边导航轨道重新设计（styles.css + AppSidebar.vue）
- 问题：切换器"个"字像箭头、激活态白块 oversized、"+"按钮溢出 64px 轨道、角标棕红色与紫底冲突、网格不统一。
- 方案：统一 40px 磁贴网格（logo/切换器/导航/+/设置），圆角 12px；激活态改 20% 白玻璃磁贴 + 3px currentColor 左指示条（新 token --sidebar-rail-active-bg/--sidebar-rail-active-on，深浅主题各一套）；切换器个人=IconUser、团队=首字反白磁贴；"+"改虚线磁贴垂直堆叠在切换器下方；角标 #f2555a + 2px 轨道色描边；切换器组与导航间加 16% 白发丝分隔线；移动端 ≤680px 隐藏切换器行。
- vite build 通过。

## 设计方案：个人/团队任务页统一（仅方案，未改码）
- 结论：不直接复用个人页（团队语义不同：分派推导状态、权限分级、富文本正文），但应共用界面骨架。
- 方案三阶段：①抽 TaskRow / TaskListGrouped / TaskDetailPanel 通用组件（纯重构）；②TeamTasksPage 接入三栏骨架（视图：我负责/我创建/进行中/已完成；快速创建收起大表单）；③补右键菜单/搜索/排序。
- 原则：组件共用、store 分离，语义差异靠 props 显式传入。

## 设计语言总结与多主题兼容
- 用户要求沉淀侧边栏设计语言并推广全项目：核心=「彩色轨道上的白色磁贴」——40px 磁贴网格、白色透明度材质（78%文字/14%悬停/20%激活/16%分隔线）、激活态 3px 指示条、圆角仅 6/12/999 三档、字重建议收敛 450/620/700 三档、彩色元素每屏≤3 处、Tabler 线性图标 stroke 1.8（尺寸 13/17-19/24 三档）。
- 主题机制：theme.ts 12 套主题内联写 CSS 变量到 <html>；default 主题跟随系统 prefers-color-scheme。
- 兼容性：白透明度材质与轨道色无关，天然兼容全部主题；已把 --sidebar-rail-active-bg/--sidebar-rail-active-on 收进 theme.ts 的 lightSurface/darkSurface（themeVariableNames 自动包含），修复手动"夜间"主题下激活态回退浅色的问题。
- 用户偏好：默认纯文字回复，不主动输出图示。
