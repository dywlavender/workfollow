# WorkFollow 任务工作台改造与验收方案

## 目标和边界

本次改造学习录屏中的高密度任务工作台、原地新增、分层弹层、文档式详情和即时反馈，不复制滴答清单的品牌资产。保留 Vue 3、Pinia、Tiptap、FastAPI 和现有数据语义。

所有阶段共享响应式验收矩阵：

| 视口 | 预期布局 | 硬性要求 |
| --- | --- | --- |
| 1440×900 | 全局导航 + 任务导航 + 列表 + 详情 | 四区同时可用，列表可调宽 |
| 1280×800 | 全局导航 + 任务导航 + 列表，详情覆盖 | 不产生水平滚动 |
| 1024×768 | 任务导航抽屉 + 列表，详情覆盖 | 触发器可见，点击遮罩可关闭 |
| 768×1024 | 同上 | 弹层不超出视口 |
| 390×844 | 底部全局导航 + 单列任务 + 全屏详情 | 44px 最小触摸区，安全区不遮挡 |

## 阶段 1：自适应任务工作台

- 分离全局导航和任务域导航的职责。
- 恢复任务视图、协作、清单和记录导航。
- 宽屏四区，中屏抽屉，小屏全屏详情。
- 保留列表拖动调宽及键盘调宽。

验收：类型检查、生产构建、五视口无横向溢出，抽屉及详情可通过键盘关闭。

## 阶段 2：统一任务命令栏

- 新增框中直接编辑日期、时间、提醒、重复、优先级、清单、标签和指派人。
- 自然语言识别只是默认值，用户可原地修正。
- Enter 创建，Escape 逐层关闭，创建后可继续输入。

验收：单元测试覆盖载荷组合；弹层在所有视口内可见；完整创建流程可用键盘完成。

## 阶段 3：结构化任务详情

- `contentJson` 作为权威正文，HTML 摘要作为兼容和搜索字段。
- 完成检查项、附件、标签和关联命令的真实行为，移除假占位。
- 使用任务级串行保存，防止旧请求覆盖新内容。

验收：正文刷新后不丢失；快速切换任务不串内容；失败状态可见并可重试。

## 阶段 4：即时联动和完整交互状态

- 创建、标题、优先级、日期、摘要和完成状态局部更新。
- 补齐骨架屏、空状态、行内错误、操作回滚和减少动效模式。
- 完成列表与详情的键盘导航。

验收：网络失败不伪造成功；联动不全量闪烁；`prefers-reduced-motion` 下无必要动画。

## 阶段 5：整体回归和视觉验收

- 运行前端类型检查、单测和构建。
- 运行后端任务、重复、协作、权限及安全回归。
- 用真实浏览器按五个视口走完新增、排期、编辑、完成和恢复。
- 将截图与验收记录放入 `output/playwright/`。

